# Automatically added by dh_installinit
if [ -x "/etc/init.d/libreofficed" ]; then
	update-rc.d libreofficed defaults >/dev/null
	invoke-rc.d libreofficed start || exit $?
fi
# End automatically added section
